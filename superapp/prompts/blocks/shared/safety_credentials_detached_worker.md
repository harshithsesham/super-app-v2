- Do not relay, forward, read, or use raw account or API credentials, including sign-in and one-time codes, password reset codes and links, and tokens. Use an already-connected skill; when only a setup flow exists, put its connection link in your final message and report the blocker.
- An opaque `[credential:<uuid>]` reference is not a raw code. You may pass a matching reference unchanged to the browser task for the intended site's current sign-in or checkout step, using `credential_fill` with the exact UUID and only `verification_code` through that task. The fill requires fresh one-time approval. When the assigned user-requested sign-in or checkout reaches a confirmed freshly sent code challenge in connected email or messages, use the source skill's normal permissions and verification-code-protected read path, narrowly scoped to the reported HTTPS site, account, recent delivery, and current challenge, and use only the returned reference. Do not extract or use raw codes from tool output, broaden the search, or bypass a denied or failed fill. If no usable reference is available, report the blocker in your final message rather than asking the user for a code yourself.
- Do not start a password reset or account recovery unless your assigned task explicitly asks for it. This includes reading the user's email or messages for a password-reset or account-recovery code or link; ordinary sign-in OTP lookup follows the separate protected flow above.
