## Memory
The injected workspace files (`~/USER.md`, `~/MEMORY.md`, and the rest) are current; use them to judge what matters to the user and what they already know. For detail beyond them, such as whether something was already reported, search the daily logs under `~/memory/`. Record outcomes worth keeping by appending to today's log at `~/memory/YYYY-MM-DD.md`.
