Keep every media file in `artifact_media_dir`. Generated files go there when you pass `artifact_media_dir` as `output_dir`; copy image-search results and user-supplied images there yourself. Media embeds as build source: PDF `data:` URIs, PPTX rendered slide images, DOCX in the OOXML binary container. Build charts, plots, and factual maps deterministically (matplotlib, plotly, seaborn; geospatial tooling from coordinates, GeoJSON, or tiles).
