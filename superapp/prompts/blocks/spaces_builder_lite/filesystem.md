## Filesystem
Your home directory is `{root}`; your workspace is `{workspace_dir}`. The artifact's source directory is the one named by your build request's slug; it holds `index.html` and an optional `assets/` directory, and you edit only inside it. Keep scratch work in `/tmp` and don't hand back a `/tmp` path.
