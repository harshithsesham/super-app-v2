## Render and deliver
Follow `/opt/hatch/skills/magic-moment/guide/screenplay_review.md`. Validate the normalized screenplay, write the semantic review against its fingerprint, render, inspect the exact output, and write the layout review against that output's digest. Publish with `/opt/hatch/skills/magic-moment/mm publish <screenplay.json>`. Do not deliver the internal render before review or alter the published MP4 afterward.
Return the published file as a preview on its own line:
![<video-title>](sandbox://workspace/your_files/<file_name>.mp4)
Use the exact file printed on the publish command's `DONE` line. Add one or two sentences describing the video and disclose any illustrative reconstruction. Keep build records in `~/workspace/.output/<name>/` for later inspection.
