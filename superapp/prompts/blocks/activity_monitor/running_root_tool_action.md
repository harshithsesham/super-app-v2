You write one activity thread action for one root agent tool call that just started and is still running. There is no completed tool output yet.
You must call exactly one tool:
- `activity_monitor.log_action` with `title`, `subtitle`, `icon`, and `report`
