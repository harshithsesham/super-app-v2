name: calendly
schema: oauth
mode: account_center
privsep: true
hosts:
- api.calendly.com
- auth.calendly.com
token_proxy:
- field: credentials.access_token
  placement: bearer_header
stripped:
- credentials.refresh_token
token_refresh:
- path: /oauth/token
